## [unreleased]

### 🚀 Features

- Achieve 100% compliance with comprehensive development tooling

### 💼 Other

- Updated readme file
- User manual
- Agents
- Added files
-  .editorconfig
- Added new tools
- DocScan Team <team@example.com>

### 🛡️ Security

- Achieve 100% compliance with comprehensive development tooling
- Achieve 100% compliance with comprehensive development tooling
- Achieve 100% compliance with comprehensive development tooling
- Achieve 100% compliance with comprehensive development tooling
- Achieve 100% compliance with comprehensive development tooling
- Achieve 100% compliance with comprehensive development tooling
- Achieve 100% compliance with comprehensive development tooling
- Achieve 100% compliance with comprehensive development tooling
- Achieve 100% compliance with comprehensive development tooling
- Achieve 100% compliance with comprehensive development tooling
- Achieve 100% compliance with comprehensive development tooling
- Achieve 100% compliance with comprehensive development tooling
- Achieve 100% compliance with comprehensive development tooling
## [1.0.0] - 2026-06-28

### 🚀 Features

- Implement MVP — extractor, inference, CLI, schemas, tests

### 💼 Other

- Initial commit
- Phase 1 scaffold: README, spec, stubs, pyproject.toml, LICENSE
- Implement MVP — extractor, inference, CLI, schemas, tests
- Implement MVP — extractor, inference, CLI, schemas, tests
- Implement MVP — extractor, inference, CLI, schemas, tests
- Implement MVP — extractor, inference, CLI, schemas, tests
- Implement MVP — extractor, inference, CLI, schemas, tests
- Implement MVP — extractor, inference, CLI, schemas, tests
- Implement MVP — extractor, inference, CLI, schemas, tests
- Implement MVP — extractor, inference, CLI, schemas, tests
- Add GitLab CI pipeline with OpenCode runner
- Merge branch 'master' of https://code.swecha.org/Gvs_Anirudh/cpu_first_hackathon
- Merge branch 'main' into temp-main
- # Conflicts:
- #	README.md
- Lint -> type-check -> security -> test -> commit-quality
- Align pipeline to 10 required jobs across 5 stages
- Align pipeline to 10 required jobs across 5 stages
- Align pipeline to 10 required jobs across 5 stages
- Align pipeline to 10 required jobs across 5 stages
- Align pipeline to 10 required jobs across 5 stages
- Align pipeline to 10 required jobs across 5 stages
- Fix shell compatibility — replace source with '.' for sh/dash
- Fix shell compatibility — replace source with '.' for sh/dash
- Fix shell compatibility — replace source with '.' for sh/dash
- Fix shell compatibility — replace source with '.' for sh/dash
- Fix shell compatibility — replace source with '.' for sh/dash
- Fix shell compatibility — replace source with '.' for sh/dash
- Rewrite for PowerShell runner on Windows
- Rewrite for PowerShell runner on Windows
- Rewrite for PowerShell runner on Windows
- Rewrite for PowerShell runner on Windows
- Rewrite for PowerShell runner on Windows
- Rewrite for PowerShell runner on Windows
- Rewrite for PowerShell runner on Windows
- Rewrite for PowerShell runner on Windows
- Rewrite for PowerShell runner on Windows
- Rewrite for PowerShell runner on Windows
- Rewrite for PowerShell runner on Windows
- Rewrite for PowerShell runner on Windows
- Rewrite for PowerShell runner on Windows
- Fix Windows PowerShell pipeline — Python 3.12 system PATH
- Fix Windows PowerShell pipeline — Python 3.12 system PATH
- Fix Windows PowerShell pipeline — Python 3.12 system PATH
- Fix Windows PowerShell pipeline — Python 3.12 system PATH
- Fix Windows PowerShell pipeline — Python 3.12 system PATH
- Fix pipeline issues — yamllint config, secret_scan, missing files, coverage threshold
- Fix pipeline issues — yamllint config, secret_scan, missing files, coverage threshold
- Fix pipeline issues — yamllint config, secret_scan, missing files, coverage threshold
- Fix pipeline issues — yamllint config, secret_scan, missing files, coverage threshold
- Fix pipeline issues — yamllint config, secret_scan, missing files, coverage threshold
- Add 10-check pipeline all passing locally on Windows PowerShell runner
- Add 10-check pipeline all passing locally on Windows PowerShell runner
- Add 10-check pipeline all passing locally on Windows PowerShell runner
- Add 10-check pipeline all passing locally on Windows PowerShell runner
- Add 10-check pipeline all passing locally on Windows PowerShell runner
- Add 10-check pipeline all passing locally on Windows PowerShell runner
- Add 10-check pipeline all passing locally on Windows PowerShell runner
- Add 10-check pipeline all passing locally on Windows PowerShell runner
- Add 10-check pipeline all passing locally on Windows PowerShell runner
- Add 10-check pipeline all passing locally on Windows PowerShell runner
- Add 10-check pipeline all passing locally on Windows PowerShell runner
- Hackthon_3 (Windows PowerShell, python hardcoded path)
- Add 10-check pipeline all passing locally on Windows PowerShell runner

### 📚 Documentation

- Restructure spec.md to follow spec-kit 7-step workflow

### 🧪 Testing

- Align pipeline to 10 required jobs across 5 stages

### ⚙️ Miscellaneous Tasks

- Align pipeline to 10 required jobs across 5 stages
- Fix runner tag and remove YAML anchors
- Fix shell compatibility — replace source with '.' for sh/dash
- Rewrite for PowerShell runner on Windows
- Fix Windows PowerShell pipeline — Python 3.12 system PATH
- Fix YAML parse error — single line pip install, no backticks
- Rewrite pipeline using Python helper scripts for PowerShell compat
- Switch runner tag from viewpoints to hackthon_3
- Fix pipeline issues — yamllint config, secret_scan, missing files, coverage threshold
- Fix before_script order — activate venv before installing packages
- Reconfigure runner to use shell executor instead of docker
- Replace venv activation with PATH-based approach to avoid PowerShell execution policy issues
- Escape PowerShell dollar signs with  to prevent GitLab variable expansion
- Use py -3.11 for SYSTEM-accessible Python, fix StrEnum, yamllint max-length
- Use absolute Python 3.11 path for SYSTEM access
- Fix PowerShell call operator & for
- Quote & in YAML to prevent anchor parsing
- Hardcode python path, fix runner config, task scheduler persistence
- Fix runner tag from viewpoints to hackthon_3
- Disable pipelines temporarily
- Add 10-check pipeline all passing locally on Windows PowerShell runner

### 🛡️ Security

- Fix runner tag and remove YAML anchors
- Fix runner tag and remove YAML anchors
- Fix runner tag and remove YAML anchors
- Fix runner tag and remove YAML anchors
- Fix runner tag and remove YAML anchors
- Fix runner tag and remove YAML anchors
- Fix runner tag and remove YAML anchors
